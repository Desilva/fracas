@model IEnumerable<StarEnergi.Models.MasterOCEntity>

@{
    ViewBag.Title = "OC";
}

<h2>@ViewBag.Title</h2>

@(Html.Telerik().Grid<StarEnergi.Models.MasterOCEntity>()
        .TableHtmlAttributes(new { style = "width:695px" })
        .Name("OC")
        .DataKeys(keys =>
        {
            keys.Add(p => p.id);
        })
        .ToolBar(commands => commands.Insert().ButtonType(GridButtonType.ImageAndText).ImageHtmlAttributes(new { style = "margin-left:0" }))
        .DataBinding(dataBinding =>
        {
            dataBinding.Ajax()
                .Select("_SelectAjaxEditing", "MasterOC")
                .Insert("_InsertAjaxEditing", "MasterOC")
                .Update("_SaveAjaxEditing", "MasterOC")
                .Delete("_DeleteAjaxEditing", "MasterOC");
        })
        .Columns(columns =>
        {
            columns.Bound(o => o.oc_description).Title("Description");
            columns.Bound(o => o.oc_value).Title("Value");
            columns.Bound(o => o.oc_score).Title("Score");
            columns.Command(commands =>
            {
                commands.Edit().ButtonType(GridButtonType.Image);
                commands.Delete().ButtonType(GridButtonType.Image);
            }).Width(80).Title("Commands");

        })
        .Pageable(p => p.PageSize(15))
        .Sortable()
        .Groupable()
        .Filterable()
        .Editable(editing => editing.Mode(GridEditMode.InLine))
        .Scrollable(c => c.Enabled(true))
        .Selectable()
) 