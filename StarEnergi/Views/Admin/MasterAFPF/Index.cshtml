@model IEnumerable<StarEnergi.Models.MasterAFPFEntity>

@{
    ViewBag.Title = "AFPF";
}

<h2>@ViewBag.Title</h2>

@(Html.Telerik().Grid<StarEnergi.Models.MasterAFPFEntity>()
        .TableHtmlAttributes(new { style = "width:695px" })
        .Name("AFPF")
        .DataKeys(keys =>
        {
            keys.Add(p => p.id);
        })
        .ToolBar(commands => commands.Insert().ButtonType(GridButtonType.ImageAndText).ImageHtmlAttributes(new { style = "margin-left:0" }))
        .DataBinding(dataBinding =>
        {
            dataBinding.Ajax()
                .Select("_SelectAjaxEditing", "MasterAFPF")
                .Insert("_InsertAjaxEditing", "MasterAFPF")
                .Update("_SaveAjaxEditing", "MasterAFPF")
                .Delete("_DeleteAjaxEditing", "MasterAFPF");
        })
        .Columns(columns =>
        {
            columns.Bound(o => o.afp_description).Title("Description");
            columns.Bound(o => o.afp_value).Title("Value");
            columns.Bound(o => o.afp_score).Title("Score");
            columns.Command(commands =>
            {
                commands.Edit().ButtonType(GridButtonType.Image);
                commands.Delete().ButtonType(GridButtonType.Image);
            }).Width(80).Title("Commands");

        })
        .Pageable(p => p.PageSize(15))
        .Sortable()
        .Groupable()
        .Filterable()
        .Editable(editing => editing.Mode(GridEditMode.InLine))
        .Scrollable(c => c.Enabled(true))
        .Selectable()
) 