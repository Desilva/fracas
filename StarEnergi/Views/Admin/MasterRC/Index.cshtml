@model IEnumerable<StarEnergi.Models.MasterRCEntity>

@{
    ViewBag.Title = "RC";
}

<h2>@ViewBag.Title</h2>

@(Html.Telerik().Grid<StarEnergi.Models.MasterRCEntity>()
        .TableHtmlAttributes(new { style = "width:695px" })
        .Name("RC")
        .DataKeys(keys =>
        {
            keys.Add(p => p.id);
        })
        .ToolBar(commands => commands.Insert().ButtonType(GridButtonType.ImageAndText).ImageHtmlAttributes(new { style = "margin-left:0" }))
        .DataBinding(dataBinding =>
        {
            dataBinding.Ajax()
                .Select("_SelectAjaxEditing", "MasterRC")
                .Insert("_InsertAjaxEditing", "MasterRC")
                .Update("_SaveAjaxEditing", "MasterRC")
                .Delete("_DeleteAjaxEditing", "MasterRC");
        })
        .Columns(columns =>
        {
            columns.Bound(o => o.rc_description).Title("Description");
            columns.Bound(o => o.rc_value).Title("Value");
            columns.Bound(o => o.rc_score).Title("Score");
            columns.Command(commands =>
            {
                commands.Edit().ButtonType(GridButtonType.Image);
                commands.Delete().ButtonType(GridButtonType.Image);
            }).Width(80).Title("Commands");

        })
        .Pageable(p => p.PageSize(15))
        .Sortable()
        .Groupable()
        .Filterable()
        .Editable(editing => editing.Mode(GridEditMode.InLine))
        .Scrollable(c => c.Enabled(true))
        .Selectable()
) 