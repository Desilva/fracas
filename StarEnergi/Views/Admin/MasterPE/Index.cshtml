@model IEnumerable<StarEnergi.Models.MasterPEEntity>

@{
    ViewBag.Title = "PE";
}

<h2>@ViewBag.Title</h2>

@(Html.Telerik().Grid<StarEnergi.Models.MasterPEEntity>()
        .TableHtmlAttributes(new { style = "width:695px" })
        .Name("PE")
        .DataKeys(keys =>
        {
            keys.Add(p => p.id);
        })
        .ToolBar(commands => commands.Insert().ButtonType(GridButtonType.ImageAndText).ImageHtmlAttributes(new { style = "margin-left:0" }))
        .DataBinding(dataBinding =>
        {
            dataBinding.Ajax()
                .Select("_SelectAjaxEditing", "MasterPE")
                .Insert("_InsertAjaxEditing", "MasterPE")
                .Update("_SaveAjaxEditing", "MasterPE")
                .Delete("_DeleteAjaxEditing", "MasterPE");
        })
        .Columns(columns =>
        {
            columns.Bound(o => o.pe_description).Title("Description");
            columns.Bound(o => o.pe_value).Title("Value");
            columns.Bound(o => o.pe_score).Title("Score");
            columns.Command(commands =>
            {
                commands.Edit().ButtonType(GridButtonType.Image);
                commands.Delete().ButtonType(GridButtonType.Image);
            }).Width(80).Title("Commands");

        })
        .Pageable(p => p.PageSize(15))
        .Sortable()
        .Groupable()
        .Filterable()
        .Editable(editing => editing.Mode(GridEditMode.InLine))
        .Scrollable(c => c.Enabled(true))
        .Selectable()
) 